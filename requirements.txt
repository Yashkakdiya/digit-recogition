     # CPU-only, Python 3.13 compatible
# Streamlit app dependencies
streamlit==1.27.0       # Python 3.13 compatible
numpy==1.26.4           # Python 3.13 compatible
Pillow==10.0.1          # Streamlit compatible
tensorflow==2.14.0      # Python 3.13 compatible (CPU only)
